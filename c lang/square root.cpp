#include<stdio.h>
#include<math.h>


int main()
{
	int a;
	printf("enter the no:");
	scanf("%d",&a);
	float b= sqrt(a);
	printf("the square root of the given no is: %f",b);
	return 0;
	
}
