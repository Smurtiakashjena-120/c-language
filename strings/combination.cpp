//A splitting machine is given a character array as an input , the splitting machine splits the character
//array in all possible methods to make individual
#include<stdio.h>
#include<string.h>
int main(){
	
	return 0;
}